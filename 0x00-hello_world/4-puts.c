#include<stdio.h>
/**
 * main - Prints message to terminal
 *
 * Return: 0 if succesful
 */

int main(void)
{
	char str[60];

	strcpy(str, "Programming is like building a multilingual puzzle");
	puts(str);

	return (0);
}
