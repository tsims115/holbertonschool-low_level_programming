#include<stdio.h>

/**
 * main - prints message to terminal using printf command
 *
 * Return: 0 if runs succesfully
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");

	return (0);
}
