#include "main.h"
/**
 * mul - takes two numbers and returns the product
 * @a: first number to be multiplied
 * @b: second number to be multiplied
 *
 * Return: the product of the two given numbers
 *
 */

int mul(int a, int b)
{
	return (a * b);
}
